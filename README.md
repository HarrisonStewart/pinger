# pinger
A PowerShell Script to quickly ping a list of hostnames to determine which hosts are currently on the network.

Reads from computers.txt and outputs a log to a nested directory. Meant to be used alongside my msi installer script and other similar AD administration scripts.

I also created a parallelized version for if you are using PS7
